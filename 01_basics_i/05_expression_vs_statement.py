# expression is something that produces a value
# 100 is an expression which has a value 100
# iq / 5 is an expression which has a value 20
# which is evaluated from (100 / 5)

# statement is the complete line including the expression

# in the following example
# 100 is an expression
# iq = 100 is a statement
iq = 100

# iq / 5 is an expression
# user_age = iq / 5 is a statement
user_age = iq / 5